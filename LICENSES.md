# License and parameter inventory

Complete inventory for the submission: model checkpoints, datasets, runtime
dependencies, and our own code. Dataset row-level detail lives in
`data/manifests/LICENSES.md`; this file is the single place a reviewer can see
everything at once.

*(Assembled by Claude under PROTOCOL §6 relay while Codex was limit-blocked;
`data/manifests/LICENSES.md` is Codex's and is unmodified.)*

## 1. Model checkpoints

| Model | Role | Parameters | License | Source | Redistributed here? |
|---|---|---:|---|---|---|
| Community Forensics 384 (ViT-S/16) | Primary expert | 21,811,969 | **MIT** (code + weights) | [`OwensLab/commfor-model-384`](https://huggingface.co/OwensLab/commfor-model-384), revision `6076002bf0d9dd37537f965ee2f06f826c333b61` | No — downloaded at runtime from the Hugging Face Hub |
| Reliability/fusion router (MLP) | **Our contribution** | ~2,000 | Same as this repository | Trained by us | Yes (weights are small) |

**Total: ~21.8M parameters**, against the brief's `<2B` limit.

Notes:
- The primary expert is **frozen**: never fine-tuned, never adapted. We claim no
  authorship of it.
- The trainable portion of this system is roughly **0.01%** of its parameters.
  The contribution is the decision layer, not scale.
- Checkpoints are not committed (`.gitignore` excludes `checkpoints/`, `models/`,
  and Hub caches). The exact revision is pinned in code and recorded in every
  run manifest, so a rerun resolves the same weights.

### Considered and not used

| Model | Why not | License |
|---|---|---|
| LOTA (ICCV 2025) | Pretrained weights are published only through a login-walled cloud drive with no public mirror. Excluded on reproducibility grounds: a dependency reviewers cannot download is one we should not ship. | MIT (code) |

## 2. Datasets

| Dataset | Use | License / terms | Redistributed here? |
|---|---|---|---|
| COCO **train2017** | Real images, smoke set | [COCO Terms of Use](https://cocodataset.org/#termsofuse) | No — fetched by script; raw images git-ignored |
| SID-Set (`label=1`) | Synthetic images, smoke set | CC BY 4.0 (attribution required) | No — streamed by script; raw images git-ignored |
| WildFake reference subset | **Sealed** organizer reference | Organizer-provided | No — and never used for any fitting |

Row-level terms, revisions, and redistribution policy: `data/manifests/LICENSES.md`.

**Two hard data rules, enforced in code rather than by convention:**
1. COCO **val2017 never** appears in any training or fitting source — asserted
   against every path and metadata field.
2. The sealed WildFake reference subset is excluded from every fitting step by a
   SHA-256 denylist that **aborts the job** on a single hit rather than skipping
   the row, because a silent skip hides a contaminated manifest.

## 3. Runtime dependencies

All permissively licensed and installed from PyPI via the committed lockfile;
none are vendored into this repository. **Values below are read from the
installed packages' own metadata** (`importlib.metadata`), not from memory, so
they can be re-derived on any machine.

| Package | Declared license |
|---|---|
| torch | Apache-2.0 AND Apache-2.0 WITH LLVM-exception (as declared by the 2.13 wheel) |
| torchvision | BSD |
| timm | Apache-2.0 |
| Pillow | MIT-CMU |
| NumPy | BSD-3-Clause AND 0BSD AND MIT AND Zlib AND CC0-1.0 |
| huggingface_hub | Apache-2.0 |
| safetensors | Apache-2.0 |
| Gradio | Apache-2.0 |
| PyYAML | MIT |
| imagehash | BSD-2-Clause |
| pytest | MIT |

Regenerate this table with:

```bash
.venv/bin/python -c "from importlib.metadata import metadata; \
[print(p, metadata(p).get('License-Expression') or metadata(p).get('License')) \
 for p in ['torch','torchvision','timm','pillow','numpy','huggingface-hub', \
           'safetensors','gradio','pyyaml','imagehash','pytest']]"
```

## 4. Demo and documentation assets

- All figures and charts are generated by our own code from committed artifacts.
- Golden-test fixtures are **synthetic images generated by
  `scripts/make_golden_sources.py`** — no third-party content enters the test
  suite.
- No third-party trademarks or logos appear in the demo or video assets.
- The organizers' brief PDF and screenshots are **not committed** (see
  `.gitignore`); this repository quotes only what is necessary for context.

## 5. This repository

See `LICENSE`. Our own contributions are the pipeline, adapters, evaluation
protocol, router, calibration, and application code.

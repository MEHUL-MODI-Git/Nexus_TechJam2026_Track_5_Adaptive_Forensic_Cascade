# Python environments and generated bytecode
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Secrets and local configuration
.env
.env.*
!.env.example

# Raw datasets, checkpoints, and model/download caches
data/raw/
data/downloads/
data/smoke/images/
data/corpus/images/
checkpoints/
models/
*.pth
*.pt
# ...but OUR OWN router head is a submission deliverable, not bulk weights: 17 KB
# against the 21.8M-parameter expert checkpoints this rule exists to exclude.
# Judges must be able to load the model that produced the reported number.
!results/router-fitting-v2/router.pt
# R1 (Codex review 2026-08-29): configs/predict.yaml points at these, so a clean
# clone cannot run the system we describe unless they are tracked. Both are our
# own small owned weights, not bulk model files.
!results/router-fitting-v2/router_reliability.pt
!results/degradation/classifier.pt
*.ckpt
.cache/
**/huggingface/
**/hub/

# Derived features and large run artifacts
data/features/
data/feature_cache/
# Results: keep the small auditable artifacts (JSON/CSV/Markdown), exclude the
# bulky row dumps. A blanket `results/**` cannot be undone by negations further
# down — git will not descend into an excluded directory, so `!results/**/*.json`
# never matched and every artifact the README links to was silently untracked
# (Codex review R6).
results/**/*.jsonl
results/**/*.parquet
results/**/*.npy
results/**/*.png
predictions.json

# Demo recordings and rendered media
recordings/
demo-recordings/
*.mp4
*.mov

# Editors and operating systems
.DS_Store
.idea/
.vscode/
*.swp

# Local tooling
.coverage
htmlcov/

# Organizer-supplied material (added by Claude [relay], task 1.6).
# The brief PDF and its screenshots are TikTok's material. This repository
# becomes public, so redistributing them is a call for Mehul, not for an agent
# to make silently. Ignoring them is the conservative, reversible default:
# delete these two lines if the decision is to include them.
Brief/
docs/evidence/

# Sealed organizer reference subset: hashed for the denylist, NEVER trained on,
# NEVER committed. Images live here only long enough to fingerprint them.
data/sealed/

# Hugging Face cache, kept inside the repo rather than ~/.cache (Mehul, 27 Aug).
# Re-downloadable; never committed.
data/hf_cache/
data/torch_cache/
third_party/
data/corpus/canonical/
logs/

# Derived local index of the organizers' sealed subset (paths + hashes, 2.8 MB).
# Regenerate with the enumeration step in scripts/sealed_reference_run.py's docstring.
# The hashes themselves are already published in data/manifests/sealed_denylist.txt.
data/manifests/sealed_files.json

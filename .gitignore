# Python environments and generated bytecode
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Secrets and local configuration
.env
.env.*
!.env.example

# Raw datasets, checkpoints, and model/download caches
data/raw/
data/downloads/
data/smoke/images/
checkpoints/
models/
.cache/
**/huggingface/
**/hub/

# Derived features and large run artifacts
data/features/
data/feature_cache/
results/**
!results/.gitkeep
!results/**/*.json
!results/**/*.csv
!results/**/*.md
predictions.json

# Demo recordings and rendered media
recordings/
demo-recordings/
*.mp4
*.mov

# Editors and operating systems
.DS_Store
.idea/
.vscode/
*.swp

# Local tooling
.coverage
htmlcov/

# Organizer-supplied material (added by Claude [relay], task 1.6).
# The brief PDF and its screenshots are TikTok's material. This repository
# becomes public, so redistributing them is a call for Mehul, not for an agent
# to make silently. Ignoring them is the conservative, reversible default:
# delete these two lines if the decision is to include them.
Brief/
docs/evidence/

# handoffs/ — large in-context analyses that must survive session death

Name: `YYYY-MM-DD_<topic>.md`. Add one row to the owning workstream's STATE.md "Read next" table. Never required reading at session start. Rename with prefix `archive_` once consumed.

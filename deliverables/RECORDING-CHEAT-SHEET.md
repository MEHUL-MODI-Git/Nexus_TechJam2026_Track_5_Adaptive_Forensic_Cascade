# RECORDING CHEAT SHEET — Track 5

> **Version 3 · snowboarder hook · 11 moments.** If your Moment 3 does not say
> `board_jpeg_q70.png` and `0.0993`, you are looking at a stale copy — close the file and
> reopen it from disk. This is the only recording document; `video-script.md` and
> `RECORD-THE-VIDEO.md` were deleted so nothing can compete with it.

**Read this file only.** Ten MOMENTS. Record each one as its own clip, splice in CapCut.
A fumbled take costs one moment, not the video.

**Framing:** static. No zooming, no scrolling mid-sentence. All zooms, highlights and text are
added in CapCut afterwards — see the POST-PRODUCTION TABLE at the end.

**Length:** the SAY lines total about 4 min 15 s at a normal speaking pace, plus roughly 33 s of
on-screen waits inside clips (three uploads, the stress test, the terminal). Call it **4 min 48 s
finished**. Because you splice, the time spent switching tabs between moments is cut and does not
count.

**Before each session, ask me to run pre-flight.** I restart the app on the current build, reset
demo state, and verify every EXPECT below against the live screen. You never run setup commands.

| you need open | how |
|---|---|
| **Tab 1 — the demo** | `http://127.0.0.1:7860`, windowed, **125% zoom**, address bar visible |
| **Tab 2 — the slides** | `deliverables/video-slides.html`, press **F** for fullscreen |
| **Terminal** | font 18pt+, already `cd`'d into the repo |

Record with Cmd+Shift+5 → Record Entire Screen.

**FRAMING RULE for the demo moments.** The page is taller than the screen, so before you start
speaking, scroll once so the part you are describing fills the frame. Scroll *before* the SAY, never
during it. Each moment says where to park the view. At 125% the verdict card and the three score
boxes fit together, which is the frame that matters most.

---

## MOMENT 1 — The problem · TAB 2, slide 2

**DO:** Show slide 2. Do not click anything.

**SAY:**
> "Anyone can fake a photo in seconds now, so platforms need to catch them automatically. Detectors
> do this well — on clean images. But no picture online stays clean; every app recompresses what you
> upload.
> We tested one of the best open detectors on three thousand images it had never seen. On clean ones
> it catches seventy-one of every hundred AI fakes. After mild noise: fewer than one in a hundred.
> It goes blind."

**EXPECT:** two large numbers, **71.1%** left and **0.7%** in orange right, with an arrow between.

---

## MOMENT 2 — The easy case · TAB 1

**DO:**
1. Drag `deliverables/video-assets/board_clean.png` onto the upload box.
2. Click **Analyze image**. Wait for the panel to fill.
3. **Scroll so the verdict card is centred.** Then start speaking.

**SAY:**
> "This is our system. This snowboarder does not exist — the photo was generated by an AI, and I'm
> uploading it exactly as it was made.
> Both the standard detector and ours get it right. That's the easy case, and everybody passes it."

**EXPECT:** **AI-GENERATED**, `p_fake` **0.9625**, and the line
`Primary CF-384 alone: 0.7070 → after router correction: 0.9625 (+0.2555)`. **No yellow box.**

---

## MOMENT 3 — Where the old detector breaks · TAB 1

**DO:**
1. Scroll up, drag `deliverables/video-assets/board_jpeg_q70.png` onto the upload box (replaces it).
2. Click **Analyze image**. Wait for the panel to fill.
3. **Scroll so the verdict card is centred.** Then start speaking.

**SAY:**
> "Now the same photo, saved as a normal JPEG — the compression every app applies. You cannot see any
> difference.
> But look at the standard detector on its own: it has dropped to zero point zero nine. Under a half
> means 'real', so it has been fooled — it now thinks this is a genuine photograph.
> Ours doesn't fall for it. We take that same broken score, notice the picture was compressed, and
> correct it back to ninety-one percent AI-generated — confidently, with no hedging."

**EXPECT:** **AI-GENERATED**, `p_fake` **0.9062**. The line
`Primary CF-384 alone: 0.0993 → after router correction: 0.9062 (+0.8069)`.
`Self-assessed reliability: 0.927`. `Detected image history: JPEG compression (91% confidence)`.
**No yellow DEFERRED box** — this one is a confident win.

---

## MOMENT 4 — How it works · TAB 2, slide 3

**DO:** Switch to slides, show slide 3.

**SAY:**
> "Here's why that's fixable. A detector alone can't tell 'this looks real' from 'this got damaged on
> the way here'.
> So before judging the picture we measure what was done to it — how blurry, how compressed, how
> noisy. The frozen expert gives its opinion, and our correction layer — one thousand eight hundred
> and twenty-seven numbers — combines the two and corrects the verdict."

**EXPECT:** five numbered rows: Canonical decode, Frozen expert — Community Forensics 384, Damage
descriptors, **Reliability router — the contribution**, Verdict + confidence grade. The bottom two
are brighter.

---

## MOMENT 5 — It checks its own answer · TAB 1

**DO:**
1. Back to the demo. The compressed snowboarder is still loaded — do not re-upload.
2. Scroll to **Stress-test this image** and click it. Wait ~3 seconds.
3. **Park so the certificate text and the bar chart are both visible.** Then speak.
4. After the SAY, expand **Stress results table** and hold on the FLIPPED rows in silence.

**SAY:**
> "A detector that's confidently wrong is dangerous — you can't tell a good answer from a bad one.
> So the system checks itself: it makes twenty damaged versions of this picture, re-runs its verdict
> on each, and counts how many still agree.
> The twenty are the organisers' own list: four JPEG qualities, three blurs, three noise levels, two
> resizes, six colour shifts, a crop, and the original.
> Eighteen survive. It grades itself medium confidence, and tells you what that's worth: answers this
> solid were right ninety-five percent of the time."

**EXPECT:** `Verdict retention 18 / 20 stress conditions`, `Forensic reliability: MEDIUM — verdicts at
this retention were correct for 94.9% of held-out sources`, a bar chart, and in the table exactly two
rows marked **FLIPPED**: `resize_0.25` and `bright_+20`.

**The 20 conditions, if you are asked** (they are the official grid, and the table lists them in this
order): `clean` · `jpeg_q90` `q70` `q50` `q30` · `blur_s0.5` `s1.0` `s2.0` · `resize_0.5` `0.25` ·
`noise_s0.02` `s0.05` `s0.10` · `bright_-20` `+20` · `contrast_-20` `+20` · `saturation_-20` `+20` ·
`crop_0.8`.

---

## MOMENT 6 — And when it genuinely isn't sure · TAB 1

**DO:**
1. Scroll up, drag `deliverables/video-assets/bird_jpeg_q30.png` onto the upload box.
2. Click **Analyze image**. Wait, then **scroll so the yellow box at the bottom is visible.**
3. Then speak.

**SAY:**
> "One more, compressed much harder. Here the system does something different.
> It still answers — ninety-five percent AI. But it knows heavy compression is where its answers have
> been least reliable, so it refuses to stand behind this one and asks for a human.
> That's the difference between confident and merely loud."

**EXPECT:** **AI-GENERATED**, `p_fake` **0.9458**, `Self-assessed reliability: 0.788`, and a
**yellow DEFERRED box**: "The system declines to decide this image and recommends human review",
with `reliability 0.7885 below the frozen policy threshold 0.8661`.

---

## MOMENT 7 — The result, and the fair version · TAB 2, slides 4 then 5

**DO:** Show slide 4, speak, press **→**, show slide 5, speak.

**SAY (slide 4):**
> "Across the damage it handles worst, the standard detector catches twelve percent of AI images.
> Ours catches eighty-three."

**SAY (slide 5):**
> "But we have to be honest. That detector was far more cautious, so part of our lead is just us
> being less careful.
> So we gave it our exact false-alarm rate and let it tune on the test answers, which we never
> allowed ourselves. Even then it reaches thirty-three percent. We're ahead by forty-nine points.
> That's the number we publish."

**EXPECT (slide 4):** **12%** orange, arrow, **83%** green.
**EXPECT (slide 5):** heading "We handicapped ourselves, and still report the smaller number", then
**33%** orange, arrow, **83%** green, then "+49.2 points, CI95 [+47.5, +50.8]".

---

## MOMENT 8 — Every kind of damage · TAB 2, slide 6

**DO:** Press **→** to slide 6.

**SAY:**
> "Every kind of damage, including where we help least. Compression and noise are where the standard
> detector collapses, and where we gain most. Overall accuracy, ninety-one percent."

**EXPECT:** a seven-row table. The bottom row `noise` is highlighted orange and reads 12.3% → 82.6%,
gain +70.3.

---

## MOMENT 9 — The organisers' own data · TAB 2, slide 7

**DO:** Press **→** to slide 7.

**SAY:**
> "The organisers gave every team a reference set. We sealed it on day one and never tuned on it — a
> score on data you've tuned against means nothing.
> We ran it once, after the freeze. A hundred and seventy-four thousand tests, zero failures — and it
> beat our own numbers: eighty-eight percent on the hardest damage."

**EXPECT:** heading "On the organizers' own data, it beat our own numbers", then three boxes:
**0.9964**, **87.9%**, **1.58%**.

---

## MOMENT 10 — Bulk use and reproducibility · TERMINAL

**DO:** Copy-paste this whole block and press enter.

```
.venv/bin/python scripts/infer_dir.py deliverables/video-assets --output predictions.json && .venv/bin/python scripts/run_eval.py --config configs/frozen.yaml
```

**SAY:**
> "It also runs over a whole folder, which is how you'd use it in bulk.
> And every table in this video re-checks itself against the file it came from, in one command.
> Ten of ten, nothing drifted."

**EXPECT:** `found 4 image(s)`, then `4 scored, 0 failed`. Then ten lines beginning `OK`, and
`10 verified, 0 verified with absent inputs, 0 drifted, 0 missing`.

---

## MOMENT 11 — Close · TAB 2, slide 8

**DO:** Press **→** to slide 8.

**SAY:**
> "Twenty-two million parameters — one percent of the size limit — and a tenth of a second per image
> on a laptop.
> A score you can price is worth more than a score you can't."

**EXPECT:** heading "A score you can price is worth more than a score you can't", footer
`21,814,571 parameters · 1.09% of the 2B cap · ~135 ms per image on a laptop`.

**FINAL CARD (add in CapCut, not recorded):** your public repo link, and on its own line:
`Synthetic sample image: SID-Set, CC BY 4.0`

---

# POST-PRODUCTION TABLE — overlays to add in CapCut

Lower third, one at a time, plain text. Each appears when the thing it names appears on screen.
Every line below is reproducible from a tracked artifact in the repo.

| # | when it appears | overlay text (verbatim) | backed by |
|---|---|---|---|
| 1 | M1, on the two big numbers | `71.1% clean → 0.7% after noise` | `results/internal-test/results.json` |
| 2 | M2, on the verdict | `Both detectors agree: AI-generated` | live UI |
| 3 | M3, as you say "zero point zero nine" | `Standard detector: 0.0993 — fooled` | live UI |
| 4 | M3, one second later | `Ours: 0.9062 — correct` | live UI |
| 5 | M3, on "no hedging" | `Confident — no human needed` | live UI |
| 6 | M4, on the highlighted row | `Our contribution: 1,827 parameters` | `results/ops/ops-evidence.json` |
| 7 | M5, on the certificate | `18 of 20 stress tests still agree` | live UI |
| 8 | M5, two seconds later | `Grade MEDIUM = 94.9% accurate` | `results/robustness/retention-signal.json` |
| 9 | M6, on the yellow box | `Heavier compression: it defers instead` | live UI |
| 10 | M7 slide 4 | `Worst case: 12% → 83%` | `results/internal-test/results.json` |
| 11 | M7 slide 5 | `Baseline tuned in its own favour: 33%` | same |
| 12 | M7 slide 5, after | `+49.2 points, CI95 47.5–50.8` | same |
| 13 | M8 | `Overall accuracy 90.9%` | same |
| 14 | M9 | `174,380 tests, 0 failures` | `results/sealed/reference-results.json` |
| 15 | M9, after | `Sealed set: 87.9% on hardest damage` | same |
| 16 | M10, on the last line | `10 of 10 published tables verified` | `configs/frozen.yaml` |
| 17 | M11 | `21,814,571 params = 1.09% of limit` | `results/ops/ops-evidence.json` |

**Do not add any overlay not on this list.** If you want another number on screen, ask me and I
will check whether an artifact backs it.

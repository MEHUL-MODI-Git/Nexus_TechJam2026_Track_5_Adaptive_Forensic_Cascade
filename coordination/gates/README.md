# gates/ — phase-exit evidence packets

One file per gate: `phase-<n>-<workstream>.md`. Contents: what was built, evidence (test output, metric tables, file paths), exit-test result vs `06-build-plan.md`, known gaps, proposed next-phase split. Reviewed by the non-owner agent (PROTOCOL.md §3). Superseded packets get renamed with prefix `archive_`.
